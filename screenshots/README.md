# Minh chứng triển khai

- `local-fallback.png`: stack Docker Compose gồm Nginx, ba replica `chat`, Redis
  và kết quả kiểm tra endpoint local.
- `railway-healthz.png`: public endpoint Railway trả trạng thái ứng dụng `ok`.
- `railway-readyz.png`: public endpoint Railway trả `ready` và Redis kết nối thành công.
